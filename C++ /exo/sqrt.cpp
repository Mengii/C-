#include <iostream>
#include <cmath>

int main(){
    using namespace std;
    
    double area;
    cout<<"Enter area : ";
    cin>>area;
    double side=sqrt(area);
    cout<<"side = "<<side<<endl;

    return 0;
}
